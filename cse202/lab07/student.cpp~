#include "person.h"
#include "student.h"
#include <vector>

using namespace std;

Student::Strudent(string n, string a, string t, string e, string st) : Person(n, a, t, e)
{
   this->status = st;
}

string Student::get_status()
{
   return status;
}
